# Only needed for custom builds with your own signing key.
# APK users don't need this - the app ships with pre-configured credentials.
#
# See README.md "Building from Source" for setup instructions.
WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
